const chai =require('chai');
const assert = chai.assert;
cost stringigyUrl = require("./app")

let test ={
    endpoints: "http://local"
}

var chai =require("chai");
var assert = chai.assert;

var  = require("./app.js");

describe('return unique items in more then one arr', function (){

	it("combine both arrays", function(){

		assert.equal(([arr1],[arr2]), [...arr,...ar2])

	})
