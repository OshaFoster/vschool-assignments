import React from "react"
import Counter from "./Counter"
import ContactList from "./contacts/ContactList"

function App(){

        return(
            <div>
            <Counter/>
        <ContactList/>
</div>
        )
}

export default App
//make a list comp
//get contact list from redux
//map through list, and render item comp for each one

//make an item component
//display item info
